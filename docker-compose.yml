version: "3.8"

services:
  frontend:
    build:
      context: .        # build from frontend folder
      dockerfile: Dockerfile
    container_name: docxtract-frontend
    ports:
      - "3000:80"       # frontend available at http://<EC2-IP>:3000
    restart: always
